[project]
name = "vpnctl"
version = "0.1.0"
description = "Small SSH-driven WireGuard VPS manager"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
vpnctl = "vpnctl.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
